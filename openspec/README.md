# OpenSpec — MyMind

Spec-driven workspace for **MyMind** (`https://github.com/askred-dot-ru/mymind`).

Not the 1C portfolio OpenSpec at `z/openspec/` (УчетЧасов / Почта CFE). Do not mix.

```
openspec/
├── project.md
├── specs/                 # current behaviour after archive
└── changes/<name>/        # proposal, design, tasks, delta specs
```

Workflow: propose → apply → archive. Human-facing product docs stay Russian; this folder follows the same change layout as Fission-AI OpenSpec.
