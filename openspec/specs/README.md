# Specs — source of truth

One folder per capability, each with `spec.md`. Requirements use `### Requirement:` and Gherkin `#### Scenario:`.

Updated only via archive of `../changes/<name>/specs/` deltas.
